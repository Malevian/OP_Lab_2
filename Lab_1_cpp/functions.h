#pragma once
#include <string>
#include <vector>

using namespace std;

vector<string> create_lines();
string create_text(vector<string>);
void input_file(string, string);
void give(string, string, string);
void output_file(string);
vector<string> sort_file_lines(string);